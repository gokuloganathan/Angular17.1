import { Component } from '@angular/core';
// import { interval } from 'rxjs';
// import { toObservable, toSignal } from '@angular/core/rxjs-interop';

@Component({
  selector: 'app-signals-and-rx-js',
  standalone: true,
  imports: [],
  templateUrl: './signals-and-rx-js.component.html',
  styleUrl: './signals-and-rx-js.component.css'
})
export class SignalsAndRxJsComponent {
  // counterObservable = toObservable(100);

  // counter = toSignal(this.counterObservable,{initialValue:0})
  
}
